# Post-Review Follow-Up Items (2026-02-24)

After reviewing recent commits (Feb 17-24, 2026), the following items need attention or follow-up. Most relate to the iOS App Store resubmission workflow and release readiness.

---

## 1. iOS screenshot still needs replacement (blocks App Store resubmission)

Commit `325f1c5` renamed the screenshot file (`ios-claude-code.png` to `ios-slash-commands.png`), but the **PNG content is unchanged** -- it still shows branded UI. The CLAUDE.md Mac checklist documents the steps:

1. Run the app on iPhone 17 Pro simulator
2. Open the slash command panel
3. Screenshot the keyboard with no third-party branding visible
4. Replace `website/public/screenshots/ios-screenshots/ios-slash-commands.png`

**This must be done on the Mac with Xcode.**

---

## 2. App Store resubmission blocked on screenshot + build bump

The code changes for the 3.2.2 rejection are done (category renames in `56c11fd`, Gemini CLI swap in `c495696`, security fixes in `9462ccc`), but the actual resubmission requires:

- [ ] Replace screenshot content (item 1 above)
- [ ] Bump `CURRENT_PROJECT_VERSION` in `ios/project.yml`
- [ ] Archive and upload via `ios/scripts/build.sh`
- [ ] Submit for review (with the 3.2.2 clarification note from the Mac checklist)

See CLAUDE.md "Mac checklist" for the full step-by-step.

---

## 3. Red-team security assessment -- review open findings

Commit `3b2d007` added a comprehensive red-team assessment at `docs/security/red-team-assessment-2026-02-21.md`. This should be reviewed to confirm all findings have been addressed or triaged. Some fixes landed in `9462ccc` (host key verification, input validation) and `04fc2a9` (store security hardening), but it is unclear if the assessment has remaining open items.

- [ ] Review `docs/security/red-team-assessment-2026-02-21.md` for unresolved findings

---

## 4. iOS build number not yet bumped

The Mac checklist calls for incrementing `CURRENT_PROJECT_VERSION` in `ios/project.yml` before the next TestFlight upload. This has not been done yet and should happen as part of the resubmission workflow (item 2).

---

## 5. Google Play track still set to `internal`

The lite flavor is publishing to the `internal` test track via CI (`PLAY_TRACK` default in `.github/workflows/build.yml`). The CLAUDE.md notes say to change to `production` when ready. Worth deciding if the lite flavor is ready for public release on Google Play.

- [ ] Decide: change `PLAY_TRACK` to `production` or keep on internal?
