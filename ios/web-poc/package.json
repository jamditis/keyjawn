{
  "name": "keyjawn-web-poc",
  "version": "0.1.0",
  "description": "KeyJawn web terminal PoC — WebSocket SSH relay with custom keyboard",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "postinstall": "mkdir -p public/lib && cp node_modules/@xterm/xterm/css/xterm.css public/lib/ && cp node_modules/@xterm/xterm/lib/xterm.js public/lib/ && cp node_modules/@xterm/addon-fit/lib/addon-fit.js public/lib/xterm-addon-fit.js && cp node_modules/@xterm/addon-web-links/lib/addon-web-links.js public/lib/xterm-addon-web-links.js"
  },
  "dependencies": {
    "express": "^4.18.3",
    "ssh2": "^1.16.0",
    "ws": "^8.18.0",
    "@xterm/xterm": "^5",
    "@xterm/addon-fit": "^0.10",
    "@xterm/addon-web-links": "^0.11"
  },
  "engines": {
    "node": ">=18"
  }
}
