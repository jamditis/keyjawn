---
import Base from '../layouts/Base.astro';
import PricingCard from '../components/PricingCard.astro';
---

<Base
  title="Pricing -- KeyJawn"
  description="KeyJawn Lite is free. KeyJawn Full is $4, lifetime, no subscriptions, no ads, no tracking."
>
  <section class="section">
    <div class="container">
      <h1 class="page-title">Pricing</h1>
      <p class="page-intro">Free download. One-time $4 upgrade. No subscriptions, no ads, no tracking -- in any version, ever.</p>
    </div>
  </section>

  <section class="section">
    <div class="container">
      <div class="pricing-grid">
        <PricingCard
          title="Free"
          price="$0"
          features={[
            'QWERTY keyboard (3 layers)',
            'Terminal key row (Esc, Tab, Ctrl, arrows)',
            'Number row with shift hints',
            'Alt character popups on long-press',
            'Voice input with streaming transcription',
            'Clipboard history (30 items)',
            'Slash command shortcuts (built-in sets)',
            'Swipe gestures',
            'Per-app autocorrect toggle',
            'Configurable quick key',
            'Color-coded extra row',
            'Shift / caps lock',
            'No ads. No tracking. Ever.',
            'Open source (MIT)',
          ]}
          ctaText="Download free APK"
          ctaLink="https://github.com/jamditis/keyjawn/releases"
        />
        <PricingCard
          title="Full unlock"
          price="$4"
          highlighted={true}
          badge="lifetime purchase"
          features={[
            'Everything in free, plus:',
            'SCP image upload to remote servers',
            'Multi-host SSH management',
            'Encrypted credential storage (AES-256)',
            'Keyboard color themes (4 presets)',
            'Menu panel with inline settings',
            'Clipboard pinning (persistent across sessions)',
            'Custom slash command sets',
            'Tooltip toggle',
            'No ads. No tracking.',
          ]}
          ctaText="Buy full version ($4)"
          ctaLink="https://buy.stripe.com/14AeVdafC3pi9Yl8nIdnW00"
        />
      </div>

      <p class="sideload-note">Both versions are APK downloads. Install by opening the file on your Android device or via <code>adb install</code>.</p>
      <p class="fine-print">* Both versions are <a href="https://github.com/jamditis/keyjawn">open source</a>. If you can build the full APK from source, it's yours — no purchase needed. The $4 is for people who'd rather not.</p>
    </div>
  </section>

  <!-- Why $4 -->
  <section class="section" id="why-4">
    <div class="container">
      <div class="why-section">
        <h2>Why $4</h2>
        <div class="why-content">
          <p>This is a one-person project. $4 covers development costs and keeps updates coming. There are no recurring payments, no data collection, no advertising, and no investor pressure to add features that make the product worse.</p>
          <p>The code is open source either way. The lite version is a full keyboard that works. The full version adds the features that power users want for terminal work.</p>
          <p>The price goes up when an iOS version ships (it won't -- iOS doesn't support this level of keyboard customization).</p>
        </div>
      </div>
    </div>
  </section>
</Base>

<style>
  .page-title {
    margin-bottom: var(--space-sm);
  }

  .page-intro {
    color: var(--text);
    font-size: 1.1rem;
  }

  .pricing-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-md);
    max-width: 800px;
    margin: 0 auto;
  }

  .why-section {
    max-width: 640px;
  }

  .why-section h2 {
    margin-bottom: var(--space-md);
  }

  .why-content {
    color: var(--text);
    line-height: 1.8;
  }

  .why-content p {
    font-size: 1.05rem;
  }

  .sideload-note {
    text-align: center;
    color: var(--text-muted);
    font-size: 0.9rem;
    margin-top: var(--space-md);
  }

  .fine-print {
    text-align: center;
    font-size: 0.75rem;
    color: var(--text-muted);
    opacity: 0.6;
    margin-top: 0.5rem;
    line-height: 1.4;
  }

  .fine-print a {
    color: var(--text-muted);
    text-decoration: underline;
  }

  @media (max-width: 768px) {
    .pricing-grid {
      grid-template-columns: 1fr;
      max-width: 400px;
    }
  }
</style>
